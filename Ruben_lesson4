# 1. Write a Python program to calculate the length of a string.
my_string = 'Here is string for your exercise!'
print(len(my_string))

# 2. Write a Python program to get a string made of the first 2 and the
# last 2 chars from a given a string.
mytext = 'w3resource'
print(mytext[0:2] + mytext[-2:])

# 3. Write a Python program to replace 'cut' word to 'dog'
oldstring = 'I have a cut and I love it'
newstring = oldstring.replace('cut', 'dog')
print(newstring)

# 4. Write a Python program to reverse 123  to 321 in text.
# Sample String : 'I have 123 books'
# Expected Result : 'I have 321 books'
txt = 'I have 123 books'
print('I have ' + ''.join(reversed(txt[7:10])) + ' books')

# 5. Replace all occurrence of word five to one.
oldstring = 'five five was a race horse, two two was one too.'
newstring = oldstring.replace('five', 'one')
print(newstring)

# 6. Write a Python program to check whether a specified value is
# contained in a group of values.
print(3 in [1, 5, 8, 3])
print(-1 in [1, 5, 8, 3])

# 7. Write a Python program to solve (x + y) * (x + y).
# Test Data : x = 4, y = 3
# Expected Output : (4 + 3) ^ 2) = 49
x = 4
y = 3
result = (x + y) * (x + y)
print(f"({x} + {y}) ^ 2 = {result}")

# 8. Write a Python program which converts float values to integer, 
# and sum of two values, then result print with reversed order.
# Test Data: x = 2,5 , y = 13.75
# Expected Output: 51

x = 2.5
y = 13.75
z = int(x) + int(y)
revresult_z = ''.join(reversed(str(z)))
print(revresult_z)
